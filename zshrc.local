export OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES

# cexport PATH="$HOME/.rbenv/bin:$PATH"
# ceval "$(rbenv init -)"
# cexport RBENV_VERSION=

export PATH="${ASDF_DATA_DIR:-$HOME/.asdf}/shims:$PATH"

export EDITOR=nvim

set -o vi

# The real local configs
# Mostly stuffs that should not be checked-in
[[ -f ~/.zshrc.real.local ]] && source ~/.zshrc.real.local
